// top navigation
.top_nav.hidden-print
    .nav_menu
        nav
            .nav.toggle
                a#menu_toggle
                    i.fa.fa-bars
            ul.nav.navbar-nav.navbar-right
                li
                    a.user-profile.dropdown-toggle(href="javascript:;" data-toggle="dropdown" aria-expanded="false")
                        img(src="/images/img.png" alt="")
                        //- if (session && session.userName)
                            //- | #{session.userName}
                        //- else
                            | ناشناس
                        span.fa.fa-angle-down
                    ul.dropdown-menu.dropdown-usermenu.pull-right
                        li
                            //- if session && session.igap
                                //- a(href="/v1.0/admin/users/"+session.userId+"/edit") پروفایل
                            //- else
                                //- a(href="/v1.0/admin/profile"+session.userId) پروفایل
                        li
                            a(href="/v1.0/admin/logout")
                                i.fa.fa-sign-out.pull-right
                                |  خروج